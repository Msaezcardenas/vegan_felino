import Banner from '../components/Banner';
import { Wrapper } from '../Wrappers/Landing';

const Landing = () => {
  return (
    <Wrapper>
      <Banner />
    </Wrapper>
  );
};

export default Landing;
