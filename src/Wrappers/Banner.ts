import styled from 'styled-components';

export const Wrapper = styled.div`
  .banner {
    position: relative;
    height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }
`;
